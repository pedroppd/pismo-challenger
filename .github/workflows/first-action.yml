name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Realizando testes unitários
        run: echo "Testes sendo executados"
      - name: Validando código
        run: echo "IA está validando o código"
      - name: Realizando deploy
        run: echo "Realizando deploy"